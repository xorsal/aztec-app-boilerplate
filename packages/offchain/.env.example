# Aztec node URL (default: local sandbox)
AZTEC_NODE_URL=http://localhost:8080

# Deployed contract address (set after running `yarn deploy`)
CONTRACT_ADDRESS=

# L1 RPC URL for sandbox (optional, defaults to http://127.0.0.1:8545)
L1_RPC_URL=http://127.0.0.1:8545
